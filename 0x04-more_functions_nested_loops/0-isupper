#include "main.h"
#include <stdio.h>

/**
 * _isupper - check the code.
 * @c: input integer variable
 * Return: 1 if c is uppercase & 0 if c is lowercase.
 */
int _isupper(int c)
{
	int i, k;

	for (i = 'A'; i <= 'A'; i++)
	{
		if (c == i)
		{
			k = 1;
		}
		else
			k = 0;
	}
	return (k);
}
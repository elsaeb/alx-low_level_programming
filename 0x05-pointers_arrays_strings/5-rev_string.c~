include "main.h"

/**
 * rev_string - reverse string
 * @s: input string
 * Return: nothing
 */
void rev_string(char *s)
{
	int i;

	for (i = 10; i >= 0; i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
